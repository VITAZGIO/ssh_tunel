<!--
Текст для страницы релиза на GitHub. Копировать целиком: этот комментарий
при вставке не отображается.
-->

## ssh_tunnel 1.0.0

Свой прокси через собственный Linux-сервер по обычному SSH. Один файл, без
установки, без прав администратора.

### Что скачивать

| Система | Файл |
|---|---|
| Windows 10 / 11 | `ssh_tunnel.exe` — окно с кнопкой и значком у часов |
| Linux (обычные серверы) | `ssh_tunnel_linux` |
| Linux ARM (Raspberry Pi и подобное) | `ssh_tunnel_linux_arm64` |

### Что умеет

- **Три протокола сразу** — SOCKS5, SOCKS4/4a и HTTP CONNECT: разные программы
  умеют разное, и нужны все три.
- **Разделение трафика по программам** — через туннель идёт всё, только
  выбранные приложения, или всё кроме выбранных. Правила меняются на ходу, без
  переподключения.
- **Локальная сеть остаётся доступной** — роутер, NAS, Home Assistant, рабочие
  сервисы и узлы mesh-VPN идут напрямую, а не через сервер. Кнопка «Проверить
  сеть» в настройках заранее скажет, сломается что-нибудь или нет.
- **Работает там, где системный прокси бессилен** — Node.js, Python, Go и всё
  на них (npm, pip, curl) читают только переменные окружения, и программа их
  прописывает.
- **Переживает обрывы связи** — пул SSH-соединений с проверкой живости и
  переподключением. Заснувший ноутбук не ломает туннель.
- **Возвращает настройки как было** при любом закрытии, включая аварийное:
  интернет после выхода не пропадает.
- **Проверяет ключ сервера** — подмена сервера по дороге не пройдёт незаметно.
- **Меряет реальную скорость** — тест в несколько потоков через туннель.
- **Видно, кто ходит в интернет** — живой список программ и адресов, с пометкой,
  если DNS-запрос ушёл мимо туннеля.

### Windows

Запустить двойным щелчком. Программа не подписана сертификатом (это стоит денег
и требует юрлица), поэтому браузер может обозвать файл опасным, а Windows —
предупредить о неизвестном издателе: «Подробнее» → «Выполнить в любом случае».

Чтобы не спорить с браузером, файл можно скачать командой — тогда он не получит
метку «загружено из интернета» и предупреждения не будет вовсе:

```powershell
curl.exe -fLO --ssl-no-revoke https://github.com/VITAZGIO/ssh_tunel/releases/latest/download/ssh_tunnel.exe
Get-FileHash .\ssh_tunnel.exe -Algorithm SHA256
```

Вторая команда печатает контрольную сумму — сверь с `SHA256SUMS.txt` ниже.

Дальше: настройки (шестерёнка), адрес сервера, круглая кнопка. Крестик прячет
программу в трей, туннель продолжает работать.

### Linux

```bash
curl -LO https://github.com/VITAZGIO/ssh_tunel/releases/latest/download/ssh_tunnel_linux
chmod +x ssh_tunnel_linux
./ssh_tunnel_linux -host ТВОЙ_СЕРВЕР -user tunnel -save
./ssh_tunnel_linux -web      # тот же интерфейс на 127.0.0.1:47821
```

Служба systemd и установщик — в `packaging/linux`.

### На сервере

Ставить ничего не надо, хватает штатного `sshd`. Но если сервер только что
арендован, стоит закрыть вход по паролю и включить firewall — готовый набор
команд одним блоком лежит в
[SERVER_SETUP.md](https://github.com/VITAZGIO/ssh_tunel/blob/main/docs/SERVER_SETUP.md).
Там же — как завести пользователя, которому можно только пробрасывать
соединения: если ключ с компьютера утечёт, сервер этим ключом не откроют.

### Чего туннель не покрывает

- Программы со своим сетевым стеком, игнорирующие и системный прокси, и
  переменные окружения — часть игр, некоторые мессенджеры.
- UDP целиком: SSH пробрасывает только TCP.
- DNS-запросы приложений, которые резолвят имена сами, до обращения к прокси.
  Такие соединения помечаются в журнале.
- Локальную сеть — намеренно, чтобы домашние и рабочие сервисы оставались
  доступны.

### Контрольные суммы

Лежат в `SHA256SUMS.txt` рядом с файлами.

Лицензия MIT. © 2026 Vitaliy (VITAZGIO). Разработано в паре с Claude (Anthropic).
